// Passo 1: Definir as variáveis fixas
const nota1 = 9;
const nota2 = 8;

// Passo 2: Calcular
const soma = nota1 + nota2;
const media = soma / 2;

// Passo 3: Saída
console.log('A média das notas é: ' + media);